{
  "name": "pm-os-portfolio-demo",
  "version": "0.5.0",
  "private": true,
  "description": "Security and accessibility checks for the PM-OS portfolio demo",
  "scripts": {
    "check": "node scripts/check-source.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
