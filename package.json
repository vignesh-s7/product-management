{
  "name": "productowner-skill-portfolio-demo",
  "version": "0.5.0",
  "private": true,
  "description": "Security and accessibility checks for the productowner-skill portfolio demo",
  "scripts": {
    "checksums": "node scripts/generate-checksums.mjs",
    "check": "node scripts/check-source.mjs && npm run test:skills",
    "sync-skills": "node scripts/sync-skills.mjs",
    "test:skills": "node --experimental-strip-types --test tests/skills/memory-contract.test.ts",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
